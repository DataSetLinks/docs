---
title: "API Reference"
---

Welcome to the DataLinks API. These endpoints let you bring in data, connect it intelligently, and ask questions that yield grounded results. Use this index to jump to the right section and understand how the parts fit together.

## How the API fits together

- **Ingest**: create datasets and load data.
- **Interconnect**: discover and manage links between datasets.
- **Inquire**: query clean, connected data for answers.

## API Reference Index

#### datasets

| | |
|---|---|
| <Badge color="green">GET</Badge> | [Fetch datasets](/api-reference/datasets/fetch-datasets) |
| <Badge color="green">GET</Badge> | [Fetch datasets in namespace](/api-reference/datasets/fetch-datasets-in-namespace) |

#### ingest

| | |
|---|---|
| <Badge color="blue">POST</Badge> | [Create new dataset](/api-reference/ingest/create-new-dataset) |
| <Badge color="blue">POST</Badge> | [Ingest data](/api-reference/ingest/ingest-data) |
| <Badge color="blue">POST</Badge> | [Preview](/api-reference/ingest/preview) |

#### dataset

| | |
|---|---|
| <Badge color="green">GET</Badge> | [Get dataset information](/api-reference/dataset/get-dataset-information) |
| <Badge color="blue">POST</Badge> | [Rename a dataset](/api-reference/dataset/rename-a-dataset) |
| <Badge color="yellow">PUT</Badge> | [Update infer definition](/api-reference/dataset/update-infer-definition) |
| <Badge color="yellow">PUT</Badge> | [Update dataset sort order](/api-reference/dataset/update-dataset-sort-order) |
| <Badge color="blue">POST</Badge> | [Clear all data for a dataset](/api-reference/dataset/clear-all-data-for-a-dataset) |
| <Badge color="red">DEL</Badge> | [Delete all data and metadata for a dataset (balefire)](/api-reference/dataset/delete-all-data-and-metadata-for-a-dataset-balefire) |

#### query

| | |
|---|---|
| <Badge color="blue">POST</Badge> | [Query data](/api-reference/query/query-data) |

#### autorag

| | |
|---|---|
| <Badge color="blue">POST</Badge> | [Run AutoRAG agent over all data](/api-reference/autorag/run-autorag-agent-over-all-data) |

#### links

| | |
|---|---|
| <Badge color="green">GET</Badge> | [Load links](/api-reference/links/load-links) |
| <Badge color="blue">POST</Badge> | [Rebuild links](/api-reference/links/rebuild-links) |
| <Badge color="blue">POST</Badge> | [Preview links](/api-reference/links/preview-links) |
| <Badge color="blue">POST</Badge> | [Add a new link](/api-reference/links/add-a-new-link) |

#### ontology

| | |
|---|---|
| <Badge color="green">GET</Badge> | [Get ontology](/api-reference/ontology/get-ontology-) |
| <Badge color="blue">POST</Badge> | [Post ontology](/api-reference/ontology/post-ontology-) |

#### schema

| | |
|---|---|
| <Badge color="green">GET</Badge> | [List all datasets](/api-reference/schema/list-all-datasets) |
| <Badge color="green">GET</Badge> | [List user namespaces](/api-reference/schema/list-user-namespaces) |
| <Badge color="green">GET</Badge> | [List datasets within namespace](/api-reference/schema/list-datasets-within-namespace) |

#### user

| | |
|---|---|
| <Badge color="green">GET</Badge> | [Get usertokens](/api-reference/user/get-usertokens) |
| <Badge color="blue">POST</Badge> | [Post usertoken](/api-reference/user/post-usertoken) |
| <Badge color="red">DEL</Badge> | [Delete usertoken](/api-reference/user/delete-usertoken) |

#### multipart-upload

| | |
|---|---|
| <Badge color="blue">POST</Badge> | [Prepare multipart upload](/api-reference/multipart-upload/prepare-multipart-upload) |
| <Badge color="blue">POST</Badge> | [Finish multipart upload](/api-reference/multipart-upload/finish-multipart-upload) |
| <Badge color="blue">POST</Badge> | [Abort multipart upload](/api-reference/multipart-upload/abort-multipart-upload) |




## Typical end-to-end flow

1. **Create a dataset**\
   Use Create new dataset. Supplying a new namespace creates it implicitly.
2. **Ingest data**\
   Use Ingest data or the multipart flow for large files. Use Preview to check parsing before committing.
3. **Review links**\
   Use Load links and Preview links. Promote useful suggestions to active links with Add a new link or from the app UI. Rebuild links when data changes.
4. **Query**\
   Use Query data to retrieve grounded results across connected datasets.
5. **Automate**\
   Use AutoRAG for agent workflows when you need plan-and-retrieve behavior at the namespace level.

## Notes and best practices

- **Namespaces**: a namespace appears when you create the first dataset that references it.
- **Large files**: prefer the multipart upload trio to improve reliability.
- **Performance**: Rebuild links after large ingests so suggestions reflect the latest data.

<Tip>
  If you're looking for background to get started, see the explanatory articles for [Ingest](/concepts/ingest-clean), [Interconnect](/concepts/interconnection), and [Inquire](/concepts/query-insights), then return here to try automating those steps using the API.
</Tip>